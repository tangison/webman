# Document Quality Standard

Lock exact text, figures, authentic logos, page size, margins, page count, and signature policy before layout.

Keep editable source authoritative. Recalculate figures outside the layout. Render every page after the latest material change. Reject clipping, overflow, missing pages, missing glyphs, broken tables, weak contrast, and repeated generic page templates.
